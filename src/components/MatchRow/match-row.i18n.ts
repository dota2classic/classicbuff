import { defineMessages } from "react-intl";
import { createI18n } from "utils/i18n";

const INTL_KEY = "dota2classic.match-row";

const messages = defineMessages({});

export default createI18n(messages);
